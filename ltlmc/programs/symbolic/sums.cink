/*

   kompile cink --transition "lbldstmt" --backend symbolic

   krun programs/symbolic/sums.cink -cIN "ListItem(#symInt(n))" -cPC "#symInt(n) <Int 3" --ltlmc "[]Ltl (L ->Ltl eqToSum(s,i)) "

*/


int n, i, s;

int main() {
//  cin >> n;
  n = 5;
  s = i = 0;
  while (i < n) {
    ++i;
    s = s + i;
    L : {}
  }
  F : {}
}

