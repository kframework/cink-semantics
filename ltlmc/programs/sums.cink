/*

   kompile cink --transition kripke --backend symbolic

   krun programs/sums.cink -cIN "ListItem(#symInt(n))" -cPC "#symInt(n) <Int 3" --ltlmc "[]Ltl (L ->Ltl eqToSum(s,i)) "

*/


int n, i, s;

int main() {
  cin >> n;
  s = i = 0;
  while (i < n) {
    ++i;
    s = s + i;
    L : {}
  }
  L : {}
}

