
/*@ \section*{New Modules}
  Two  modules are defined: \texttt{CINK-BASIC-SYNTAX} and 
  \texttt{CINK-BASIC-SEMANTICS}, including the syntax and the semantics,
  respectively, of the basic constructions. The these two modules together
  with the main configuration sre included in the main module. 
  These modules will be imported in the extensions of the language, as well.
*/

